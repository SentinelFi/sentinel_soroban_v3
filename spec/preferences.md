---
name: preferences
description: User coding preferences and conventions — loaded on every phase start and prime
type: feedback
---

- **Always use OpenZeppelin Stellar crates** when available (tokens, vaults, access control, pausable, etc.). Load the `oz-stellar` skill when working with OZ modules.
